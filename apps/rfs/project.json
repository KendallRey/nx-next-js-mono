{
  "name": "rfs",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/rfs",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project rfs --web",
  "targets": {
    "prettier": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx format:write --projects rfs"
      }
    }
  }
}

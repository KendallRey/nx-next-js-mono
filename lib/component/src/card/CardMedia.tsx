import { CardMedia, CardMediaProps } from "@mui/material";
import React from "react";

const MuiCardMedia: React.FC<CardMediaProps> = (props) => {
  return <CardMedia {...props} />;
};

export default MuiCardMedia;
